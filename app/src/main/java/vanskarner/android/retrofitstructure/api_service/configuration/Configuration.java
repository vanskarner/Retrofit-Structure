package vanskarner.android.retrofitstructure.api_service.configuration;

class Configuration {
    protected static int connectTimeoutSeconds=30;
    protected static int readTimeoutSeconds=30;
    protected static boolean enableLog=true;//Only enable it in development or maintenance
}
